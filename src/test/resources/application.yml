spring:
  datasource:
    url: jdbc:h2:file:./h2/integationtestdb
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
server:
  #端口号
  port: 8084